*** Setting ***
Library              SeleniumLibrary

*** Variable ***
${C_ROOT_URL}    http://${C_SERVER}:${C_PORT}    # Root URL for application
${C_SERVER}      localhost                       # Hostname for test environment
${C_PORT}        8080                            # Port Number for application
${C_BROWSER}     Firefox                         # Browser for Selenium test execution
${C_DELAY}       0                               # Browser test execution speed delay

*** Keyword ***